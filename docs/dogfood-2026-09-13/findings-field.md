# Findings — field workstream (daily log, delays, look-aheads, RFIs, inspections, plans, updates)

Project: Ikorodu–Sagamu Link Road Rehabilitation (Section 2) — `prj_99ea3fe0-16a7-4c83-a333-dc3ba79611f1`
Workspace: Precon QA Builders, user QA Reviewer (PM/site agent). Date of run: 13 Sep 2026. Screenshots in `scratchpad/shots/`.
Format: **severity** · page · what I did → what happened → what a PM expects. Numbering continues from findings-setup.md (last was ~#4x); this file restarts at F1.

## 1. Daily log (/daily-log)

F1. **bug (blocking)** · Daily log → day drawer → "Add conditions" / "Edit" dialog · Clicked the weather chip "Sunny" (07 Sep) → the whole dialog **submitted immediately** (PUT /daily-logs/2026-09-07) with the numbers as they stood (0/0 crew, 0 h) and **Weather saved as "—"**. Re-opened via Edit, clicked "Sunny" again → submitted again, weather still "—". Cause: the chips are `<Button>` inside the `<form>` with no `type="button"` (`components/molecules/upsert-daily-log-dialog.tsx` ~line 161), so the click is a form submit and `handleSubmit` reads the stale `weatherCondition`. **Weather can never be recorded through the UI**; the first accidental submit also created a bogus "logged" day. → A PM expects the chip to select the weather and the day to save only on "Save changes". I set weather via the API for 07–12 Sep (finding logged).
F2. **missing** · Daily log day drawer · "Activities" section is read-only ("No activities logged against this day"). There is **no control anywhere to link an activity with hours to a day** — the hook `useLinkDailyLogActivity` and route `POST /daily-logs/:date/activities` exist but no component calls them. The plan's "activities linked with hours" is impossible from the UI; the Site activity page has no "log hours today" either. Used the API for the links and recorded that here. → Expect an "Add activity" row in the drawer (activity picker + hours, defaulting to the activities planned for that date).
F3. **confusing** · Daily log list page · There is **no date picker** to log a past day. "Add my log" always opens *today*; the only way to reach 07 Sep was the "Add log" button that appears on the "Missed report" rows — which only appeared **after** the first (accidental) log existed. Before that, the list was empty with no way to choose a date except editing the URL (`?date=2026-09-07`). → Expect a date field on "Add my log" (a site agent writes up Friday on Monday morning all the time).
F4. **confusing / wrong-for-construction** · Daily log KPIs · Before any log: "Days logged 0 / Missed days 0". After the first log on 07 Sep: "Missed days 6" — every calendar day between the first log and today is "Missed report", **including today (Sunday 13 Sep, before the day has ended) and Saturday 12 Sep**. Missed-ness is anchored to the first log, not to the site possession/start date, and has no working-week calendar. Also "Days logged 1" counted the bogus 0-crew, no-weather day as logged (see F1). Screenshot after first log: the whole table is red.
F5. **bug (environment, blocks 3 workstreams)** · Daily log entry → "Attach image" (09 Sep, `site_photo_mobilisation.png`) · Toast "Could not upload image" (half hidden behind the Panda AI bubble, drawer screenshot `shots/09-daily-log-drawer-after-links.jpg`). Backend `POST /files` → **500**, dev.log: `AggregateError ECONNREFUSED 127.0.0.1:9000` — MinIO is not running (Docker is down on this Mac). Not an app bug as such, but: (a) the UI shows a 500 as a generic toast with no "storage unavailable" hint, (b) **the text I had typed into the entry editor was wiped** when the upload failed — a site agent loses a paragraph of diary because a photo failed. Every file upload in this run (plans, updates photos) hits the same 500; recorded per section.
F6. **bug** · Daily log → `?date=2026-09-15` (future) · "Add conditions" → saved 12/12 crew, 100 h for **Tuesday 15 Sep, two days in the future**, no warning, no error. The day is then **invisible** on the list page (range ends today) and cannot be voided from the UI, so a fat-fingered date becomes a phantom record. Voided it via `POST /daily-logs/2026-09-15/void` (no UI). → Reject or at least warn on dates after today; show future-dated logs in the list.
F7. **ok (by design)** · Duplicate date · A second log for the same date is impossible by construction — the day is an upsert (`PUT /daily-logs/:date`) and the drawer switches to "Edit". Multiple *entries* per day are allowed, which is right (several people log the same day). No finding.
F8. **missing validation** · Conditions dialog, 12 Sep · Set Present 8, Total hours **200** (25 h per worker) → saved silently, KPI "Total hours" jumped to 641 h. No warning at hours > workers × 12, no warning that day total (200 h) ≠ activity hours logged (48 h). Reset to 48 h afterwards. → Warn (not block) when hours/worker > 12 and show a reconciliation between day hours and activity hours.
F9. **missing** · Row "⋮" menu (View / Conditions / Download report / Email me) and day drawer · There is **no "Void day"** in the UI although `POST /daily-logs/:date/void` exists and a "Voided" tab is on the list page. Only individual entries can be voided. The Voided tab therefore always shows "No days match" unless the API is used.
F10. **good** · Void entry · Dialog requires a reason, keeps the entry visible struck-through with "Voided by … · time" and the reason, entry count still shows it (ENTRIES (2) with one voided). This is the right audit-trail behaviour. Re-added a corrected entry without issue.
F11. **confusing / wrong-for-construction** · Weekly report (Download report → Weekly, date 09 Sep → `.docx`) · Contains header, Days logged 6/7, hours 489, avg crew 9.5, weather counts (Sunny 3 / Cloudy 2 / Rain 1) and an activity-hours table — but **no diary text at all**: none of the six narrative entries, no per-day table, no "No work — site flooded at ch 1+150" for the rain day, no voided-entry trail, no crew per day. A weekly site report to the RE is *primarily* the narrative and the per-day record. Also "&" is rendered as `&amp;` in Word ("Hoarding &amp; site security") — HTML-escaped text in a docx. Also "Overall project completion 0%". The rain day is counted as a logged day with 0 crew, which is correct, but nothing in the report says why.
F12. **confusing** · Report after voiding · Voided the 09 Sep entry *after* generating the weekly report. Nothing links the report to the data it was generated from — no report history, no "report issued 13 Sep 09:33, data changed since" flag; re-downloading silently produces a different document. For a contractual record (weekly report to the RE) the issued version should be retained or at least the regeneration flagged.
F13. **cosmetic** · Daily log list · "Logged By" column shows "—" when the day has conditions but no text entry (08 Sep before I re-added the entry) even though I recorded the conditions. Also "Missed report" for today (Sunday 13 Sep) at 10:30 in the morning.
F14. **side effect to verify** · Linking activity hours via the API flipped the three mobilisation activities to **"In Progress"** (weekly report Status column) without anyone setting progress — % complete stayed 0. Checking on the Site activity page in §2.

## 2. Delays (Site activity → activity → delay)

Baseline before any delay: Site activity 32 activities, 3 "In progress" (flipped by the hour links — F14 confirmed; "Actual" start still "—"), Delayed 0. Key dates: Site possession Met, 9 "In progress". Overview: Schedule KPI (before) not captured; after delay 1 see F17.

### Scenario 1 — Weather, 10 Sep (Plant mobilisation, Hoarding & site security, 1 day each, reason Rain)
Did: Site activity → "Log a delay" on each → Reason "Rain", Started at 10 Sep 05:00, description, cost ₦0, prevention note → "Log delay".
F15. **wrong-for-construction (design gap)** · Log a delay dialog · Fields are Reason, Started at, What happened, Estimated cost impact, How can we prevent this. **There is no "days lost" / duration, no "ended at", no culpability (contractor / client / neutral), no EOT flag, no link to an RFI/CR.** A delay in this app is an open-ended incident, not a schedule event. Consequently nothing can "slip 6 days": the plan's core scenario cannot be expressed. Reason list is building-centric and has no "Utility strike / unforeseen ground conditions", no "Third party / community", no "Late RFI response" (used Other / Client Approval / Equipment Breakdown / Delivery Delay as the nearest).
F16. **bug** · Delay lifecycle · Once logged, a delay **cannot be viewed or resolved anywhere in the UI**: the row shows "1 open" as plain text (not a link), the row menu is only Edit / Delete, the Edit drawer has no delays section. `useResolveDelay` + `PATCH …/delays/:delayId` exist but no component calls them. So every delay stays "open" forever.
F17. **bug (cascade)** · After the two 1-day weather delays: (a) activity planned dates **unchanged** (07–14 Sep, 09–16 Sep), status still In progress, "Variance —"; (b) Project chart shows "Plant mobilisation · delayed" and a child bar **"Delay: Rain 10-09-2026 duration 4"** and **"Delay: Rain … duration 6"** — the open delay is drawn from its start to the activity's planned end, so a 1-day rain stop reads as a 4- and 6-day delay; "Timeline shift 0 d", "Projected end Mar 26 2027" unchanged; (c) no look-ahead existed yet; (d) key dates unchanged (correct — nothing depends on these); (e) Overview "Schedule −3 pts · Behind plan · 2 delayed activities" but **Needs attention (2 items) lists only "1 permits expiring, 1 high risks" — not the delays**; (f) the 10 Sep daily log shows nothing about the two delays raised for that day (no cross-link either way). Successors: none defined (no dependencies in the manual form, setup finding #32) so nothing could move.
F18. **cosmetic** · Project chart KPI "Daily logs 7" — counts the voided future 15 Sep day; the Daily log page says 6.
F19. **bug (minor)** · Typing a wrong schedules URL (`/schedules/chart`) shows the full-screen "Something broke on site" error boundary instead of a 404 / redirect.
F20. **cosmetic** · Key dates page · Statuses are Met / **In progress** / Missed; every future key date is "In progress" although the API says "Upcoming". A key date is not "in progress".

### Scenarios 2, 3, 4, 5, 7, 8 — logged through the same dialog (all future-dated except 4/7/8 which are also after today)
Did: "Log a delay" on Excavate & blind culvert 1 (Other, 14 Oct, ₦1.85M), Cast culvert 1 base & walls (Other, 20 Oct), Import laterite & spread (Delivery Delay, 16 Nov, ₦2.4M), TMP approval & signage (Client Approval, 18 Sep), Cut to formation (Equipment Breakdown, 8 Oct, ₦950k), Compact & CBR tests (Client Approval, 20 Sep — late RFI-002 answer), Cart away to approved tip (Other, 30 Sep, ₦420k). Then, because nothing cascades, did what a PM would do by hand: Edit drawer → Cast culvert 1 planned 26 Oct–09 Nov (+6 d), Excavate & blind culvert 1 end 19→25 Oct.
F21. **missing validation** · Log a delay · A delay "Started at" **14 Oct 2026 / 16 Nov 2026 — one and two months in the future** — is accepted without a word. A delay is a record of something that happened; a future delay is a risk. (Weather 10 Sep, TMP 18 Sep etc. were fine.)
F22. **wrong-for-construction** · Culpability / EOT · Nothing distinguishes the laterite supplier delay (contractor-culpable, no EOT) from the NNPC strike or the Ministry's late TMP approval (EOT-able). The reason list has no "client / neutral / contractor" attribution and there is no EOT register, so the plan's check "the app should NOT let a supplier delay be claimed as client delay without a flag" fails by omission — everything is just an "open delay" with an optional ₦ cost. (Reason "Delivery Delay" is the closest; I wrote CULPABLE in the description.)
F23. **bug (cascade)** · After 9 open delays and the manual +6 d on culvert 1: (a) Site activity — Excavate 12–25 Oct, Cast 26 Oct–09 Nov, no status change, "Variance —" for both (variance only appears once an *actual* date exists); (b) Project chart — "9 open delays · ₦5.6M delay cost", **Timeline shift 0 d, Projected end Mar 26 2027** — the manual +6 d on the culvert did not move "Culvert 2", "Culvert 3", the drains or anything after it (no dependencies exist, see setup #32), Gantt now shows seven more open-ended "Delay: …" child bars running to each activity's planned end (screenshot `shots/10-gantt-open-delays-no-shift.jpg`); (c) no look-ahead yet; (d) Key dates — "Culverts complete 27 Nov" and "TMP approved 18 Sep" **unchanged**, TMP approved still "In progress" though a client-approval delay of 7 days was logged against the activity that feeds it; no key date is linked to an activity at all (API `linkedActivityId` null on all 10); (e) Overview — "Schedule −3 pts · Behind plan · 9 delayed activities" (the score did **not** move between 2 and 9 delayed activities), "Cost variance ₦0" although ₦5.6M of delay cost is logged, Needs attention still only permits + risks; (f) daily logs for 30 Sep / 8 Oct / 14 Oct don't exist yet and there is no prompt to create them.
F24. **bug** · Edit site activity → "Actual start 11 Sep, Actual end 12 Sep" on *Control survey & benchmarks* (planned 14–17 Sep) → saved. Status **stays "Planned"** with an actual end date in the past, progress 0 %, "Variance −2 days". An activity with an actual finish is complete; the app should flip status (or refuse). (Did the same by accident on Cast culvert 1 — actual 11–12 Sep against a 26 Oct plan → "Planned", "−13 days" — then cleared it.)
F25. **missing** · Progress · There is **no way to set % complete anywhere**: not in the Edit drawer, not on the Site activity table (bar only), not on the Project chart (SVAR gantt is wired only for start/end drags — `use-schedule-editor.ts` persists `update-task`/`drag-task` dates; a progress drag would change the bar locally and be lost on reload). `PATCH /activities/:id {percentComplete:60}` returns 200 but percentComplete stays 0 (schema strips it). So "60 % → 40 %" cannot be enacted; "Work items 0/32 completed" and "Construction progress 0 %" will stay 0 for the life of the job unless activities are marked Completed by status.
F26. **bug (UI state)** · Site activity row "⋮" → Edit · After the first Edit in a page session, later "Edit" clicks (via the leaked, still-mounted menus — setup #26) open the **previously edited activity's drawer**, and values typed while a stale drawer is up get saved onto the wrong activity (this is how Cast culvert 1 got 11–12 Sep actuals). Reproducible with synthesised clicks; a real mouse may avoid it, but the mounted-menu leak is real: `document.querySelectorAll('[role=menu]').length` grows with every row menu opened.
F27. **cosmetic** · Site activity table · Planned "7 days" for Mon 7 – Mon 14 Sep (8 calendar days, 6 working) and "4 days" for Mon–Fri — the duration is end−start in whole days, neither calendar nor working days (see setup #33). Delay cost column header is "Delay Cost" but the cell shows "₦1,850,000.00 / 1 open" — the "1 open" count sits under the cost.

## 3. Look-aheads (/look-aheads)
Did: Add look ahead → name "Weeks 2-3: 14-27 Sep …", dates 14–27 Sep, 22 workers, ticked 8 activities, Draft → Create. View → Edit look ahead → Status "Approved" → Save changes. Then Site activity → deleted "Condition survey & photo log" (it was in the approved look-ahead).
F28. **confusing** · New look ahead · Changing the start/end dates does **not** pre-select or filter the activity list — all 32 activities are listed unfiltered (32 checkboxes) with only a planned-start date each; the PM ticks by hand. The "Coming up (13 Sep – 11 Oct)" cards above the table already know which activities fall in a window, but that auto window is not offered when creating. No "select all in range".
F29. **wrong-for-construction** · Approval · There is no approve action; "Approved" is just a value in the Status dropdown of the Edit drawer — anyone who can edit can "approve" their own look-ahead, no approver, no timestamp, no comment. A look-ahead approval is a sign-off record.
F30. **bug** · Approved look-ahead vs activity delete · Deleting the activity removed it **silently** from the approved look-ahead (Activities 8 → 7, "6 gaps" → "5 gaps"), the look-ahead stayed "Approved" with no note, and the delete confirm ("This permanently removes the activity and its logged delays") **did not mention** that an approved look-ahead referenced it. Screenshot `shots/11-approved-lookahead-after-activity-delete.jpg`. → Either block deletion while referenced by an approved plan, or record the removal on the look-ahead and drop it back to Draft/Under review.
F31. **missing (delay cascade, (c))** · The look-ahead detail lists TMP approval & signage, Plant mobilisation and Hoarding — all three have open delays — with only "Planned / InProgress"; **no delay flag** on the look-ahead, nor on the "Coming up" cards. A look-ahead is exactly where the site agent wants to see "this item is already 7 days late".
F32. **cosmetic** · "Materials" column shows "6 gaps" / every card says "No materials ordered" in red, including for TMP approval and survey work that need no materials — noise for a civils programme; the copy assumes every activity consumes ordered materials.

## 4. RFIs (/rfis)
Did: Raised RFI-1 (High, cost+schedule flags, due 20 Sep), RFI-2 (CBR, due 11 Sep so it is overdue today), RFI-3 (kerb type, Low, due 15 Oct); ball-in-court reassigned twice on RFI-2; posted the RE's answer as the official response on RFI-2; reopened RFI-2; converted RFI-1 to a change event.
F33. **missing** · Raise an RFI → Ball in court · Options are only "Unassigned (you)" and "QA Reviewer". The invited Client participant "Engr. Bolanle Adeyemi (Resident Engineer, LSMW)" (status *invited*, role Client, has `rfis: view/create`) is **not offered**, so an RFI cannot be put in the RE's court until she accepts the invite. Used the free "Notify email" field instead. → Invited participants should be selectable (the ball can be in the court of someone who hasn't logged in yet).
F34. **bug** · Raise an RFI · The Question rich-text editor is **not cleared between raises**: RFI-3's question was saved as RFI-2's full text followed by the kerb question (the Subject field *was* cleared). Screenshot of RFI-3 body in `shots/` not taken; the API shows the concatenated body. There is **no Edit RFI** in the UI to fix it; corrected via `PATCH /rfis/:id {question}` (record).
F35. **missing** · RFI detail drawer · No audit trail is shown: two ball-in-court reassignments (PATCH ×2), the reopen and the conversion all leave nothing visible in "Responses & comments" — the backend logs events (`logEvent`) but the drawer doesn't render them. For a contractual record the drawer should show "Reassigned to X by Y at T", "Reopened", "Converted to change".
F36. **wrong-for-construction** · Overdue · RFI-2 is due 11 Sep (two days ago). The list shows "Due 11 Sept" in plain grey, the drawer "Due 11 Sept 2026", the list header "3 awaiting response" — **no overdue badge, count, colour or filter anywhere**, and the Overview "Open items 0 · 0 RFIs" ignores RFIs altogether (see F40). Scenario 7 (late RFI response) is therefore invisible to the PM.
F37. **good / cosmetic** · Answer · "Post as the official response" flips status to Answered and ball-in-court back to the raiser (correct). The same text is then rendered twice — once under "Official response" and again in the comments thread.
F38. **missing** · Reopen · An Answered RFI has only "Close RFI" in the drawer; there is no Reopen. `POST …/transition {status:"Open"}` works (reopenedCount 1) but the list still showed "Answered" until reload. Used the API (record).
F39. **wrong-for-construction** · Convert to change event (RFI-1) · One click, no confirmation, no cost/days prompt, and it **created a new Draft change request "RFI-1: Culvert 1 invert level …" with cost ₦0** — the commercial workstream had already raised "CR-002 Relocate culvert 1 by 8 m to clear NNPC pipeline" (₦2,150,000 + 10 d) for exactly this. There is no "link to an existing change request", so the project now has two CRs for one event. The drawer afterwards says only "Linked to a change event" with no link to open it.
F40. **bug** · Overview KPI "Open items 0 · 0 RFIs · 0 approvals · 0 change orders" while three RFIs are open (one overdue) and three change requests exist. Checked after raising them.
F41. **cosmetic** · RFI cards are plain divs (no role/button/link) — not keyboard reachable; the drawer opens on click but sometimes closes again on a second click (toggle).

## 5. Inspections (/inspections)
Did: Request New Inspection ×2 (Formation approval proof roll 12 Nov; Culvert 1 base slab rebar 27 Oct). Board view → Move "Formation approval" → Action required (= fail), then Move → Scheduled (= reschedule).
F42. **bug (same root cause as F1)** · Request a new inspection · The Category chips (Structural / Quantity Survey / General Progress / Electrical / Plumbing) are `type="submit"` buttons inside the form: clicking "Quantity Survey" after filling the title **submitted the request immediately with Category = Structural** (the default). Category can never be chosen by mouse.
F43. **wrong-for-construction** · Inspections module · Titled "Independent Inspections & Quality Reports" with categories Structural / QS / General Progress / Electrical / Plumbing — no Earthworks / Formation / Drainage / Pavement / Materials-testing; no hold-point or ITP concept; no link to an activity or a chainage; no inspector on the project (both cards say "Inspector: Pending assignment" with nothing to click). A formation approval on a road is a **hold point** on the earthworks activity and it feeds the key date "Formation approved" — none of that is modelled.
F44. **missing validation** · Requested "Formation approval" for 12 Nov while earthworks (05 Oct–13 Nov) are 0 % and haven't started, and "Culvert 1 rebar" for 27 Oct while the culvert isn't excavated — **no warning either time**. Nothing stops you inspecting work that hasn't happened; the request isn't linked to an activity so it can't know.
F45. **missing** · Fail / reschedule · There is no Pass/Fail: the only status moves are Scheduled → "Action required" → Completed via a "Move" menu on the Board view (List view has no actions at all, cards are inert). "Action required" carries **no reason, no findings, no photo, no re-inspection date**; moving back to Scheduled keeps the original date — there is no way to change the date (no edit drawer; `PATCH /inspections/:id` accepts `scheduledAt` but the UI never calls it). Risk level is fixed at "Low" with no way to change it.
F46. **cosmetic** · List view · Cards show "Date & Time 2026-11-12" (ISO, no time) and "Category · Structural"; no sort, no upcoming-vs-overdue grouping.

## 6. Plans (/plans)
Did: Upload plan → Category "Civil / Site" → `IS2-RD-101_typical_cross_section.pdf` → Upload. Then a 0-byte `empty_drawing.pdf` as Structural.
F47. **blocked (environment)** · Upload plan · `POST /files` → **500 Internal server error** (MinIO on :9000 down, Docker not running — same as F5). Toast "Internal server error" + inline red "Internal server error" under the file. So the **review workspace, markups (comment / measurement / cloud), Rev C versioning and duplicate-file checks could not be exercised in this run** — they need the object store. Screenshot `shots/12-plan-upload-500.jpg`. Re-run §6 once `docker compose up minio` is possible.
F48. **missing validation** · Upload plan · A **0-byte PDF** is accepted client-side ("0 B · application/pdf", Upload button enabled) and posted to the server; no "file is empty" check. (Server answer masked by the 500.)
F49. **confusing** · Upload plan categories · Architectural / Civil-Site / MEP / Sample Plans / Structural / Survey — no drawing number, revision, title, scale or discipline sheet-set fields; "Size and filename are read automatically" is all the metadata. A road drawing register needs number + revision (IS2-RD-101 Rev C) as first-class fields, otherwise "upload Rev C as a new version" is a filename convention.
F50. **cosmetic** · Dialog title says "Upload document" on the Plans page (shared component with Documents).

## 7. Updates (/updates)
Did: New update ×2 ("Mobilisation complete - site established at Km 14", "Survey in progress - control network and centreline"), each with a site photo attached.
F51. **bug** · New update → Photos & videos · Selected `site_photo_mobilisation.png` / `site_photo_survey.png`: the dialog shows **no filename / thumbnail** after selection, and on "Post update" the photo upload hit `POST /files` **500** (storage) yet the update was **posted anyway with `media: []` and no error to the user** — console shows an unhandled `AxiosError 500` from `upsert-update-dialog.tsx:71`. A client-facing progress update silently loses its photos.
F52. **wrong-for-construction / noise** · Updates feed · Every activity-hours link on a daily log auto-posted a **"Progress · Site work logged on <activity> — N hour(s) logged on <date>"** update: 13 auto-updates from one week of diary before I wrote a single real update, all attributed to "QA Reviewer · Project Manager". The Updates feed is the client-facing channel (settings copy: "written for the homeowner"); an hours-link is not a stakeholder update. Expect these to be digest-only or off by default.
F53. **cosmetic** · New update categories are Progress / Material Delivery / Inspections / Issues — no "Delay / weather", no "Safety", no "Traffic management"; title placeholder "e.g. Second floor slab poured".

## 8. Panda AI — "What is delaying the project?" (delay cascade (g))
F54. **good, with a caveat** · Assistant bubble · Listed all 9 delays with activity, reason, "Days lost", "EOT: Yes/No" and cost — but every "days lost" and "EOT" value was **parsed from my free-text description** (the delay record has no such fields, F15). Ask the same question after a site agent writes "grader down again" and the answer will be "Days lost: unknown". It gave no total, did not say 14 EOT-able days, and did not mention that no dates have moved. Screenshot `shots/13-panda-ai-delays.jpg`. Also the "Panda AI" *page* (/panda-ai) is an "analysis" screen with no chat; the chat lives only in the floating bubble.

## 9. EOT claim and liquidated damages
F55. **missing** · There is no EOT register and no liquidated-damages concept anywhere (grep: no `liquidated`, `extensionOfTime` in frontend or backend). Recorded the claim as a change request with Cost ₦0 / Time +14 days ("EOT-001 …") — then found the commercial workstream had already created "CR-003 Extension of time — 14 days, no cost (EOT claim No. 1)" (Submitted), so I deleted mine. Key date "Practical completion" stays 26 Mar 2027 (Upcoming) — nothing shows a revised completion of 09 Apr 2027 or an LD exposure, so the plan's check "LDs not shown as due while an EOT is pending" is moot: LDs are never shown at all.
F56. **bug** · Deleting a change request that an RFI was converted into · Deleted the duplicate "RFI-1: Culvert 1 …" CR (Delete → confirm "This permanently removes the change order and its discussion") — no warning that RFI-1 is linked to it; RFI-1 still carries `changeRequestId` and the drawer still says "Linked to a change event" pointing at nothing.

## Data left in the project (for the other workstreams / next run)
- Daily logs 07–12 Sep (6 days, weather set via API, 13 activity-hour links via API, 7 text entries incl. 1 voided + corrected); 15 Sep phantom day voided via API.
- 9 open activity delays (cannot be resolved from the UI); Cast culvert 1 moved to 26 Oct–09 Nov, Excavate & blind culvert 1 end 25 Oct; Control survey has actual dates 11–12 Sep (status still Planned); "Condition survey & photo log" was deleted for the look-ahead test and re-created (new id).
- Look-ahead "Weeks 2-3: 14-27 Sep …" Approved, 7 activities (the re-created Condition survey is not in it).
- RFI-1 Open (converted; its generated CR deleted), RFI-2 Open (answered then reopened via API, reopenedCount 1, overdue since 11 Sep), RFI-3 Open (question fixed via API).
- Inspections: Formation approval (Scheduled, 12 Nov), Culvert 1 base slab rebar (Scheduled, 27 Oct).
- Plans: none (uploads 500). Updates: 2 manual + 13 auto "Site work logged" posts.

## Counts
Created through the UI: 6 daily-log days (conditions) + 7 diary entries (1 voided with reason, 1 corrected) + 1 weekly report (.docx) · 9 activity delays · 3 manual activity edits (2 date moves, 1 actual-dates) · 1 activity deleted + re-created · 1 look-ahead (created, approved) · 3 RFIs (1 answered, 1 converted) · 2 inspections (1 failed + rescheduled via Move) · 2 site updates · 1 EOT change request (deleted as duplicate of CR-003) · 1 Panda AI question.
Via API because the UI offers no way (each recorded above): weather on 6 days (F1), 13 activity-hour links (F2), void of the 15 Sep day (F6/F9), reopen RFI-2 (F38), fix RFI-3 question (F34), 2 progress PATCHes that were ignored (F25).
Blocked by environment: everything that uploads a file — daily-log photo, plan uploads/markups/Rev C/duplicate, update photos (MinIO :9000 down, Docker not running).

## Top 10
1. F1/F42 — Weather chips (daily log) and Category chips (inspection request) are submit buttons: clicking one submits the form; weather can never be recorded, category is always Structural.
2. F15/F16/F17/F23 — A delay has no days, no end, no culpability and cannot be viewed or resolved; nothing cascades (dates, successors, key dates, look-ahead, Gantt "Timeline shift 0 d" with 9 open delays); Gantt draws open delays to the activity's planned end.
3. F2 — No UI to link activity hours to a daily log (hook + route exist, nothing calls them).
4. F25 — % complete cannot be set anywhere (drawer, table, Gantt), and PATCH ignores it — "Construction progress 0 %" forever.
5. F34 — Raise RFI keeps the previous question in the editor: RFI-3 was saved with RFI-2's text; no Edit RFI to fix it.
6. F36/F40 — Overdue RFI has no badge/filter; Overview "Open items 0 · 0 RFIs" with 3 open RFIs and 3 CRs.
7. F30 — Deleting an activity silently drops it from an *approved* look-ahead, no warning, status stays Approved.
8. F39/F56 — "Convert to change event" creates a new ₦0 CR instead of linking CR-002; deleting that CR leaves the RFI pointing at nothing.
9. F6/F9/F8 — Future-dated daily log accepted and then invisible/unvoidable in the UI; 200 h for 8 workers accepted silently.
10. F51/F52 — Update photos silently dropped on upload failure; 13 auto "Site work logged" posts spam the client-facing Updates feed.
