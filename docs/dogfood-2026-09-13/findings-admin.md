# Admin dogfooding findings — Ikorodu–Sagamu Link Road (Section 2)

Reviewer: project manager ("QA Reviewer", workspace Precon QA Builders). Date: 13 Sep 2026.
Project: http://localhost:5173/project/prj_99ea3fe0-16a7-4c83-a333-dc3ba79611f1
Scope: Documents, Permits, Team/People, Risks, Tasks, Settings, Notifications, Panda AI, Overview.
Severity scale: bug / wrong-for-construction / confusing / missing / cosmetic.
Screenshots in `scratchpad/shots/admin/`.

Format: **severity** · page · what I did → what happened → what a PM expects · console error if any.

## 1. Documents (/documents)

1. **bug (env blocker)** · Documents → Upload document · Picked category "Government Approvals", chose IS2_traffic_management_plan.pdf (938 B), clicked Upload → red banner "Internal server error" and toast "Internal server error"; nothing uploaded. Backend log: `POST /files` → `AggregateError [ECONNREFUSED] connect ECONNREFUSED 127.0.0.1:9000` (the S3/MinIO file store is not running; Docker Desktop is stopped and cannot be started without also auto-starting the stale backend/db/frontend containers on 3000/5432/5173 under the native stack the other workstreams are using). → For the PM the message is useless: "Internal server error" tells me nothing about whether it is my file, my permission, or the server. Expect "File storage is unavailable, try again later" and the backend to return a typed 503 rather than an unhandled 500. Consequence: **no document, permit attachment, plan or photo upload can be tested in this run**; everything below about documents is from inspecting the UI, not from a persisted upload. Screenshot: `shots/admin/01-upload-500.jpg`.
2. **missing / wrong-for-construction** · Documents → Upload document → Category · The only categories are Contracts & Agreements, Government Approvals, Inspection Certs, Invoices & Receipts, Land Documents, Proposal, Sample Documents. There is nowhere sensible for a traffic management plan, a method statement, an insurance certificate (CAR / public liability), a BoQ, a specification, a QA/ITP, a survey report, a H&S file or an as-built. "Land Documents" and "Proposal" are home-build/real-estate categories. No "add category" control either. → Expect construction categories (Drawings, Specifications, BoQ/Commercial, Method statements & plans, Insurance & bonds, Permits & approvals, QA/Test certificates, H&S, Correspondence, As-builts) or user-defined categories.
3. **missing** · Documents → Upload document · Form is Category + File only: no title/description, no revision/version label, no "supersedes" pick, no date-of-document, no visibility (client-visible / internal), no link to an RFI/permit/activity. Multiple upload is not possible (`input multiple=false`). The dialog says "Size and filename are read automatically" — so the filename is the only identity, which is why "same filename twice" (edge case) matters and could not be tested.
4. **cosmetic** · Documents · Category filter combobox and search exist for an empty list; the "Status" column in the table has no legend — nothing says what statuses a document can have (approved? superseded?). The floating Panda AI bubble sits on top of the drawer's Upload button (bottom-right), so the Upload click landed on the bubble; had to submit via script.

## 2. Permits & compliance (/permits)

5. **missing / confusing** · Sidebar · There is no "Permits" entry anywhere in the project sidebar (Project / Schedules / Field Tools / Materials & Equipment / Finance / Documents / People & Admin). The page exists at `/permits` (feature flag `compliance.permits`) and I only found it by reading the router. A PM cannot discover it. Same for `/inspections`, `/change-requests`, `/people`, `/queries`, `/action-items`, `/whats-next`, `/key-dates` (the latter is reachable via Schedules). → Expect Permits & compliance in the sidebar (Field Tools or People & Admin).
6. **ok** · Permits → Add permit · Added: (a) Road closure permit, LSMW, ref LSMW/RC/2026/0417, applied 20 Aug, approved 01 Sep, expires 30 Nov 2026; (b) EIA approval, LASEPA, approved 01 Sep 2025, expired 01 Sep 2026; (c) LASTMA traffic diversion approval, approved 28 Aug 2026, valid to 28 Feb 2027. All three saved; status derived from dates: Approved / Expired / Approved; the expired one is grouped under "Needs attention — Expired 12 day(s) ago"; tabs All / Expired 1 / Active 2. Screenshot: `shots/admin/02-permits.jpg`. No console errors.
7. **missing** · Permits → Add permit · No document attachment. The plan said "LASTMA approval with the TMP attached" — there is no file/document field or link to Documents; I had to write the filename into Notes. Also no "conditions" field (a road closure permit always carries conditions: hours, lane widths, signage), no "responsible person", no "renewal due / reminder" date separate from expiry, no link to the activity it gates (TMP approval & signage) or to the key date "TMP approved". → A permit is a contractual gate; it should attach the instrument and hang off the programme.
8. **wrong-for-construction** · Permits · Status is "set automatically from these dates" with only Applied / Approved / Expired. There is no Rejected, Withdrawn, Suspended, Conditional, or "Renewal submitted" — my EIA is *expired with a renewal lodged*, which is a very different risk from "expired, nothing done", and the app can only show "Expired". No "Pending renewal" state; no way to record the renewal application date without editing away the original approval.
9. **confusing** · Permits · The road closure permit expiring 30 Nov (78 days away, i.e. before Base course/asphalt work which needs it until Mar 2027) is shown simply as "Approved — Valid until 30 Nov 2026" with no warning that it expires **before the programme ends** (project completion 26 Mar 2027). The backend's expiring window is fixed at 30 days (`EXPIRING_WINDOW_DAYS = 30` in `modules/permits/expiry-job.ts`) and is not configurable per permit. For a permit that takes 6 weeks to renew, 30 days is too late. → Expect a per-permit lead time and a "covers programme?" check against the stage/activity dates.
10. **bug (label)** · Overview → Needs attention · After adding the permits the Overview shows "1 · permits expiring". The item is the **expired** EIA, not an expiring one (reporting query counts status Applied + Expired: `whereIn("status", ["Applied","Expired"])` in `modules/reporting/service.ts` ~L182, and the tile label is "permits expiring"). The genuinely-approaching road closure permit is not counted. → Expect "1 permit expired · 0 expiring within 30 days" and the expired one to be red.
11. **missing** · Permits · No sort/filter by authority, no history (who added / edited / when), no audit of status changes, no export. Cards show Edit / Delete only — deleting a permit record on a public-works job with no confirmation-of-reason or archive is a records-management gap (see Delete test below).

## 3. Team / People (/team, /people)

12. **ok** · Team → Invite someone… · Invited 7 people: Bolanle Adeyemi (Client preset), Tunde Okafor (Project Manager), Chidi Nwosu (PM preset + custom role "Site Agent"), Amaka Eze (Materials Approver + "Quantity Surveyor"), Femi Balogun (Inspector + "Surveyor"), Musa Ibrahim (Materials Requester + "General Foreman"), Grace Obi (Inspector + "Materials Engineer"). All show as "Invited" with the custom role label. No console errors. A second browser session as the client is not possible (no password for the invitee, invite email goes nowhere locally) — skipped.
13. **wrong-for-construction / security** · Team → Invite → Client preset · The role card says "Views progress, raises queries and approves milestone payments. **Finance details hidden by default**", yet the permission matrix the preset actually ticks includes **FINANCES: View ✓, Dispute ✓** (also Daily log Report, Materials Report, RFIs Create, Approvals Decide, Selections Decide). The backend enforces `requireProjectPermission(id, "finances", "view")` on GET /finances (`modules/finances/routes.ts` L170+), so a client invited with the preset **can read the contractor's full cost/expense/invoice position** via the API or by typing the /finances URL — the "hidden" map in `invite-participant-drawer/constants.ts` (`"commercial.finances": "hidden"`) is a frontend sidebar preference, not authorization. On a public-works job the Ministry's RE must see invoices/valuations submitted to them but must **not** see the contractor's expenses, margin or budget-vs-actual. The permission model is one flat "finances.view" — there is no split between "client-facing" (contract sum, IPCs, retention) and "internal" (costs, expenses). Screenshot: `shots/admin/03-client-role-editor.jpg`.
14. **missing / wrong-for-construction** · Team → Invite → Role presets · Presets are Client, Project Manager, Architect, Inspector, Guest, Materials Requester, Materials Approver. There is no Resident Engineer / Engineer's Representative, Quantity Surveyor, Site Agent, Surveyor, Foreman, Materials/Lab Engineer, Safety Officer, Clerk of Works, Subcontractor. "Architect" is a building role. Custom role names work but they are just a label over a preset's checkboxes; nothing distinguishes "client-side" from "contractor-side" (which matters for who is ball-in-court on an RFI, who approves a valuation, who signs an inspection). There is no organisation/company on the person (LSMW vs Precon QA Builders) — I had to put it in the name.
15. **bug (cosmetic)** · Team · Avatar initials are derived from first and last tokens of the name: "Engr. Bolanle Adeyemi (Resident Engineer, LSMW)" → "EL", "Femi Balogun (Surveyor)" → "F(", "Chidi Nwosu (Site Agent)" → "CA". Should ignore titles and parentheses.
16. **bug** · Team → Invite (duplicate email) · Re-invited bolanle.adeyemi@example.com as Guest → backend returned 409 with a good message ("That person already has a pending invite to this project.") but the drawer shows the raw Axios string "Request failed with status code 409". The server message is discarded.
17. **confusing** · Team vs People · Two pages list the same participants: `/team` ("Project access" with Edit/Remove + "Contacts — for reference only, does not grant access") and `/people` (same list, Remove only, no Edit, "Invite to project"). `/people` is not in the sidebar. A PM does not know which is canonical. "Contacts" (reference-only people) is a third concept — an RE who is a Contact cannot be assigned an RFI. → One People page with access + role + company + phone.
18. **cosmetic** · Team → Invite → Permissions matrix · Group labels leak internal keys: "AC" (Create/Read/Update/Delete), "RATECARDS", "COMPLIANCEDOCS", "TAKEOFFS", and a "MORE" group with Organization / Member / Invitation / Team — org-level admin permissions offered inside a *project* invite. The matrix has 95 checkboxes with no search; the drawer is taller than the viewport and the "Send invite" button sits under the floating Panda AI bubble.
19. **missing** · Team · No resend invite, no invite expiry shown, no "copy invite link", no phone number, no per-person notification preferences, no view of "what this person will see" (preview as). Removing a participant who is ball-in-court on an RFI could not be tested because the RFIs are being created by another workstream and the participants have not accepted; noted for follow-up.

## 4. Risks (Overview → Identified Risk Factors)

20. **bug** · Overview → Identified Risk Factors · There is **no way to add a risk in the UI**. The empty state says "Add a risk factor to track and mitigate issues on this project" but there is no button; the panel component (`pages/project/overview/risk-factors-panel.tsx`) declares `createOpen` state and mounts `UpsertRiskDialog` for creation but nothing ever calls `setCreateOpen(true)`. No Risks page exists in the sidebar either (feature flag `quality.risks` and the "RISKS View/Manage" permission exist, so it is meant to). → Used the API (`POST /projects/:id/risk-factors`, same session cookie) to create the four risks: NNPC pipeline strike at culvert 1 (High), October rain delays (Medium), Bitumen price escalation (Medium), Utility relocation IKEDC/water main (High). Screenshot: `shots/admin/04a-risks-no-add-button.jpg`, then `shots/admin/04-risks-on-overview.jpg`.
21. **ok** · Overview → risk card → Edit · Changed Utility relocation from High to Low via the dialog → saved, badge turned green "Low". Delete on Bitumen → confirm dialog "This permanently removes the risk factor. This action cannot be undone." → removed. No console errors.
22. **bug** · Overview → Edit risk · The description rich-text editor opened **empty** although the risk has a description (risks created via the API — or by Panda AI — carry `description` but no `descriptionHtml`; the editor only reads the HTML). Saving kept the plain description this time, but a user would believe the description is blank and retype it.
23. **wrong-for-construction** · Overview header / KPIs · With two **High** risks open the project badge in the header still read "Low" (it is a static project `riskLevel` field, not derived from the register); the KPI row has no risk tile; and "Needs attention" kept showing only "1 permits expiring" — the snapshot does have a `risks.high` count (`modules/reporting/service.ts` counts `risk_factors` where severity=High, and `needs-attention-card.tsx` has a "high risks" row), so either the snapshot is cached or the row is dropped. Re-check at the end of the run (see Overview section). → Expect the register to drive the header risk level.
24. **missing / wrong-for-construction** · Risk record · Fields are Title, Description, Severity (Low/Medium/High) only. No likelihood × impact matrix, no owner, no mitigation/response, no status (open / mitigated / closed / occurred), no cost or time exposure, no link to the activity or stage at risk, no review date, no history. Delete is permanent with no "closed/realised" alternative — a risk that *happened* (the pipeline strike) should be closed as "occurred → became RFI 1 / CR 2", not deleted. Every card is rendered with the same red tint regardless of severity (Medium and Low look identical to High apart from the small badge).
25. **cosmetic** · Overview → risk list · Newest first, no sort by severity, no count, no filter; the list grows unbounded inside the Overview card.

## 5. Tasks (/tasks)

26. **ok** · Tasks → New task · Created "Chase TMP approval with Ministry (due 18 Sep key date)" (High, due 11 Sep 2026 — past, assigned to me), "Book CBR tests on laterite — blocked by RFI 2" (Medium, due 20 Sep, labels RFI-2/materials), "Confirm precast culvert lead time with Costain Precast" (High, due 25 Sep). Added a column "Waiting on client". No console errors. Screenshot: `shots/admin/05-tasks-board.jpg`.
27. **bug** · Tasks → New task (second open) · The description editor is **not reset between creates**: the third task was created with the second task's description prepended ("Linked to RFI 2: … For site agent Chidi Nwosu — …"). Title, due date and labels were reset; only the rich-text description leaked. A PM creating five tasks in a row would ship wrong descriptions.
28. **bug** · Tasks board · The task due 11 Sept (2 days overdue on 13 Sep) shows a neutral grey "11 Sept" chip — no overdue colour, no "overdue" text, no sort/filter for overdue, nothing on the Overview "Needs attention". A past due date on creation is accepted silently (fine) but never surfaced.
29. **wrong-for-construction** · Tasks → New task → Assignees · Only "QA Reviewer (me)" is offered. The seven people invited today (including the site agent Chidi Nwosu) are not assignable until they accept the invite. On a real job the PM assigns work to the site agent the same day he is added; "invited" people should be assignable (or at least the Contacts should be). Could not assign task 3 to the site agent — put his name in the description.
30. **confusing** · Tasks → New task vs Edit task · Linking a task to an RFI / change request / material / invoice / stage payment / action item exists only in the **Edit** dialog ("Linked items"), not on creation — so the workflow is create → reopen → link. The RFI picker showed "Nothing to link" because the field workstream had not yet raised the RFIs at that moment (`GET /rfis` = []) — retry at the end. The linked-items picker is a bare type select + "Search rfi…" combobox with no RFI numbers/status in the results format visible.
31. **bug (accessibility)** · Tasks board · Moving a card between columns is **drag-and-drop only**: no "Move to column" menu, no status/column field in the Edit dialog, and the dnd-kit keyboard sensor did not respond (focus card → Space → ArrowRight → Space did nothing; pointer drag via automation also did not register). The columns' own "Reorder" buttons *do* take keyboard focus (DndDescribedBy-0) so the board is half-accessible. Used `PATCH /tasks/:id/move` via the API to verify the move persists (see next entry).
32. **cosmetic** · Tasks · Columns are To Do / Doing / Done with "Add column"; a column can be renamed/collapsed/deleted from its header (delete with tasks in it not tested). "My tasks / All tasks" tabs only. No list view, no due-date sort, no grouping by assignee or label, no link from a task to the activity/stage it belongs to (a road PM's tasks are almost always "for activity X at ch Y").

## 6. Settings (/settings)

33. **missing (civil project)** · Settings · The whole page is three cards: Project budget (range slider ₦10M–₦10B, "Enter a custom amount"), Currency (170-entry flat select), Panda AI update drafts (Off / Daily internal digest / Weekly client update / Both — set to "Both", toast "Panda AI will draft the weekly client update and the daily internal digest", persisted). Screenshot: `shots/admin/06-settings.jpg`. **Not editable anywhere**: project name, site address / chainage (start–end km, route), project type, client (Lagos State Ministry of Works) and the engineer/RE, contractor legal entity, contract number & form (re-measure), commencement / completion dates, defects period, LD rate & cap, retention & advance (these live under Finance → contract terms, not here), working calendar (6-day week? public holidays? rainy-season allowance), site working hours, units (metric), reporting week start, who receives notifications. For a road job the working calendar is the single biggest gap: every "working days missed" and "days logged" figure on the Overview assumes a calendar nobody can see or set.
34. **confusing** · Settings → Project budget · "Estimated budget range ₦850M – ₦850M" as a dual-handle slider; the contract sum is a fixed figure and the same number is already stored as the Finance contract. Two sources of truth for the headline figure (budget vs contract sum) with no statement of which one dashboards use.
35. **cosmetic** · Settings → Panda AI update drafts · "homeowner" ×5 in the copy. The client is a Ministry; on any commercial job the client is a company. The copy also promises drafts "drawn from daily logs, activities, deliveries and RFIs" — see Panda AI section for what it can actually see.
36. **missing** · Settings · No project-level notification preferences (only the org-level page at /dashboard/settings/notifications — every type is a bell-only toggle; no email/SMS/WhatsApp channel, no digest frequency, no per-project mute). No archive / close project, no danger zone, no export.

## 7. Notifications (bell + /dashboard/settings/notifications)

37. **wrong-for-construction / missing** · Bell · After a full day of activity by four workstreams the bell holds 3 unread: "A high-severity risk was added — Utility relocation…" (8 min ago), "A high-severity risk was added — NNPC pipeline strike…" (8 min), and "Project health score dropped — Health fell from 70 to 50 on 2 bedroom Flat Interior" (7 h ago, a **different project**). Screenshot: `shots/admin/07-bell.jpg`. Nothing arrived for: the expired EIA permit or the road-closure permit, the seven invites (fine — I sent them), the overdue task (due 11 Sep), any RFI / queried invoice / delayed activity / late material order from the other workstreams (partly because those events target "the assignee" and everything is assigned to me by me, and partly because the types simply do not exist — see next). The Utility risk notification survived my downgrade to Low with no correction.
38. **bug** · Permit expiry notifications · `permit_expiring` / `permit_expired` are only produced by a once-a-day BullMQ sweep (`modules/permits/expiry-job.ts`, 30-day window) addressed to the **project owner only**; nothing fires on create/edit, so a permit entered already expired (my EIA) never notifies anyone until the next sweep, and the site agent / RE never do. A PM expects an immediate "permit already expired" on save and a lead-time reminder they can set.
39. **missing** · Notification types · The catalogue (46 types, all "On, appears in your notification bell") has no: activity delayed / slipped, look-ahead item missed, daily log missing for yesterday (the Overview already knows "Working days missed: 6"), material order late vs needed-by, key date at risk (only "approaching"/"missed"), invoice queried by client (only "vendor invoice submitted"/"invoice overdue"), inspection failed exists but "inspection due" does not, task overdue (only action items), EOT/claim events, permit condition breached. There is no notifications *page* — the bell popover is the only history, with "Mark all read" and no filtering, no per-project view, and no deep links visible in the accessibility tree.

## 8. Panda AI (project chat pane) — verbatim answers in `panda-answers-admin.md`

40. **partially wrong** · Q1 "What is outstanding on the contract?" · Answered with the contract *record*: IS2-MC-001, Pending, ₦850,000,000, legal entity LSMW, "signed document not on file", 12 phases all pending with scheduled values (42.5m / 12.75m / 34m / 93.5m / 110.5m / 93.5m — all match the plan) and "Used Material Cost: 30,800,000 NGN" on the drainage phase. It never mentioned what a PM means by "outstanding": the ₦85,000,000 mobilisation advance (IS2-ADV-001, **Paid**), IPC-001 ₦20,187,500 (**Sent**, due 14 Oct), retention held, advance recovery, remaining contract value ≈ ₦744.8m. "All phases are pending" is also wrong: Mobilisation and Survey activities are InProgress and 6 days are logged against them. Verdict: accurate on the fields it read, blind to invoices/valuations, so misleading.
41. **hallucination** · Q2 "Which material orders are late?" · Listed Precast culvert (needed 20 Oct), reinforcement (15 Oct) and cement (12 Oct) as "past their needed-by dates" — **all three are a month in the future** (today 13 Sep). It omitted the one order that *is* late: "Softwood boards 150×25", needed-by **01 Sep 2026**, still Requested (Ogun Quarries Ltd). The agent has no notion of today's date (the system prompt in `modules/panda-ai/agent/service.ts` does not state the current date, so the model guesses). A PM acting on this would chase three suppliers who are not late and miss the one that is.
42. **mostly right, one silent error** · Q3 "Summarise this week's site diary" · Workers/weather/hours per day match the logs (10/12/13/0-rain/14/8). It also reported a log for **15 September 2026 — two days in the future** (the field workstream logged it as an edge case and the app accepted it) as an ordinary day with 12 workers and 100 h, and the "week" ran 7–15 Sep. No flag that a future-dated log is suspicious; no mention of what work was done (activities), only headcounts.
43. **wrong refusal** · Q4 "What changed on culvert 1?" · "I could not find any information regarding changes on culvert 1. It appears there are no drawing markups or related data available." The project holds: two culvert-1 activities each with a delay record (Excavate & blind culvert 1 now 12–25 Oct, Cast base & walls 26 Oct–9 Nov, both 6 days later than planned), a High risk "NNPC pipeline strike at culvert 1 (ch 0+420)", and a task "Confirm precast culvert lead time". Q5 a minute later *did* return the culvert delays, so the data is reachable — the agent chose the markups tool only and gave up.
44. **right but undated** · Q5 "What is delaying the project?" · Correctly listed all 9 delay records with reason, cost impact and notes (total ₦5,320,000). It did not say that 7 of the 9 "Started At" dates are in the **future** (Oct–Nov 2026 — scenario delays entered ahead of time) and presented them as current; it did not distinguish EOT-able (weather, client approval, utility strike = 15 days) from culpable (supplier, plant) delays, nor give the net effect on completion (26 Mar 2027) — the question a PM actually asks.
45. **wrong-for-construction** · Q6 weekly client report · Structure is sensible (status, progress, site activity, delays, risks, next steps) and the figures it quotes are consistent with the pages (12 phases, 32 activities, 3 in progress, 7 logs, 3 risks 1L/1M/1H, 9 delays ₦5.32m). But it opens with **"Overall Status: On Track"** and closes "The project remains on track despite some delays" while the Overview's own schedule KPI says "Behind plan (−3 pts)", 9 open delays and an expired EIA permit. "Amount Spent: 0 NGN" ignores the ₦85m advance received and the expenses the finance workstream logged. It mentions no permits, no RFIs, no invoices/valuation, no key dates (TMP approval due 18 Sep), no look-ahead. A client-facing report that says "on track" when the system knows otherwise is exactly the document that ends up in a dispute.
46. **confusing** · Panda AI page vs pane · `/panda-ai` is only "Run analysis / No analysis yet"; the chat lives in the floating pane (which covers dialog buttons on every page). Replies take 30–60 s with no streaming indicator visible in the accessibility tree; no "sources" / links to the records it used; no way to correct it ("that order is not late").

47. **ok / cosmetic** · Tasks → Edit → Linked items (retry at end of run) · Once the field workstream's RFIs existed, the "Search rfi…" picker listed the three RFIs by title only (no RFI number, no status, no ball-in-court) and the link saved; the card now shows an "RFI" chip and the dialog lists "RFI · 1 · Confirm laterite CBR requirement… · Open" — the "1" is either a count or the wrong number (this is RFI **2**). Either way the RFI number should be shown.

(Update to #23: after the reporting snapshot refreshed later in the run, "Needs attention" did show "1 high risks" alongside "1 overdue RFIs" and "1 permits expiring", so the count is wired but lags — the header badge stayed "Low" throughout.)

## 9. Overview — final state, 13 Sep 2026 (screenshots `shots/admin/08-overview-top.jpg`, `08-overview-mid.jpg`, `08-overview-bottom.jpg`)

What it shows: header badge "Low"; KPIs — Construction progress 0% (0 of 12 phases), Schedule "−3 pts · Behind plan · 9 delayed activities", Budget used ₦0 (0% of ₦850,000,000), Cost variance ₦0 (0 categories over budget), Cash ₦20,187,500 (invoiced ₦105,187,500 · paid ₦85,000,000), Open items 3 (3 RFIs · 0 approvals · 0 change orders); Needs attention: 1 overdue RFIs · 1 permits expiring · 1 high risks; Cash-flow S-curve "No data available"; Budget vs Actual "No data available"; Programme strip of 12 stages with grey ticks; Next key dates TMP approved (5 days), Formation approved (61), Culverts complete (75); Field activity 6 of 14 days logged, 6 working days missed, average crew 10, bar chart 7–12 Sep; Health score "--"; Recommended actions "Panda AI will analyse this project shortly"; two auto-generated "SITE PHOTO" updates with no photo; three risks.

48. **Assessment — does it tell the truth?** Partly.
   - **Schedule**: "Behind plan · 9 delayed activities" is *technically* what was entered but 7 of those 9 delays are dated Oct–Nov 2026 (future scenario entries). On 13 Sep the honest statement is "1 day lost to rain on 10 Sep; TMP approval due in 5 days; culvert 1 has a 6-day slip booked". The key date "Culverts complete" is still 27 Nov although both culvert-1 activities moved 6 days — no cascade to key dates, and the completion date (26 Mar 2027) is nowhere on the page. −3 pts with no scale and health "--" gives the PM nothing to act on.
   - **Cost**: "Budget used ₦0" and "Cost variance ₦0" are **false** — a ₦6,400,000 diesel expense is recorded (`/transactions`), the contract shows variations ₦4,200,000 (adjusted contract ₦854,200,000), and the drainage phase carries ₦30.8m "used material cost" per Panda AI. The finances API even reports `certifiedGrossToDate: 0` and `amountPaidToDate: 0` while the same page's Cash tile says paid ₦85,000,000 — two finance figures for the same thing disagree on one screen.
   - **Cash**: the tile is right as a receivable (₦105.19m invoiced − ₦85m paid = ₦20.19m outstanding) but the label "Cash" reads as cash in hand. No retention held, no advance recovery, no payment due date (IPC-1 due 14 Oct) — the S-curve is empty despite a valued 12-phase programme.
   - **Open items**: "3 RFIs · 0 approvals · 0 change orders" — correct for RFIs; but 3 material approvals (2 approved, 1 rejected), a Requested equipment item, a Requested precast order and 3 tasks (1 overdue) are invisible. "0 change orders" while variations total ₦4.2m contradicts the finance record.
   - **Needs attention**: the three rows are the right idea; "permits expiring" is an *expired* permit, the overdue task and the late softwood order are missing, and the two future-dated "delayed" activities dominate the schedule KPI while the one real question (TMP approval in 5 days, gating single-lane working) is a small "Due in 5 days" chip.
   - **Field activity**: "6 of 14 days logged · 6 missed" counts calendar days before site possession (7 Sep) and Sunday 13 Sep as "missed" — there is no working calendar. The rain day (0 workers) correctly appears as a logged day with 0 crew. The 15 Sep future log is silently included in totals elsewhere (Panda AI) but not shown here.
   - **Net**: the page is honest about what was typed and dishonest about what it means — it cannot tell a rained-off day from a slipped programme, shows ₦0 spent with ₦6.4m in the ledger, and never states the completion date, EOT position or LD exposure that a road PM is actually managing.

## Summary of what was created in this workstream
- Documents: 0 (upload blocked — file store down, finding #1)
- Permits: 3 (road closure, EIA expired, LASTMA)
- Participants invited: 7 (+1 duplicate rejected 409)
- Risks: 4 created (API, no UI trigger), 1 edited High→Low, 1 deleted → 3 remain
- Tasks: 3 created, 1 column added, 2 moved (API), 1 linked to RFI 2
- Settings: Panda AI cadence set to Both
- Panda AI: 6 questions asked, answers in `panda-answers-admin.md`
