# Endpoint the "Book a consultation" form POSTs leads to (JSON body).
# Leave unset in development — submissions are logged to the console instead.
# Example: a backend route, a serverless function, or a form service (Formspree, Web3Forms).
NEXT_PUBLIC_LEADS_ENDPOINT=
